<div id="small_login">
  <div>
    您需要登录后才可以留言，请先 <%= link_to '登录', new_user_session_url(subdomain: ENV['USER_SUBDOMAIN']) %> 或 <%= link_to '免费注册', new_user_registration_url(subdomain: ENV['USER_SUBDOMAIN']) %>
  </div>
</div>
