<%= link_to t('title'), root_url(subdomain: ENV['SUBDOMAIN']), class: "navbar-brand" %>
</div>
<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav">
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="javascript:void(0);">
        夜市商品 <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <li>
          <%= link_to '最新商品', commodities_url(subdomain: ENV['SUBDOMAIN']) %>
        </li>
        <li>
          <%= link_to '商品分类', categories_url(subdomain: ENV['SUBDOMAIN']) %>
        </li>
        <li>
          <%= link_to '商家摊位', traders_url(subdomain: ENV['SUBDOMAIN']) %>
        </li>
      </ul>
    </li>
    <li>
      <%= link_to '夜市经验', articles_url(subdomain: ENV['SUBDOMAIN']) %>
    </li>
    <li>
      <%= link_to '夜市秀', posts_url(subdomain: ENV['SUBDOMAIN']) %>
    </li>
    <li>
      <%= link_to '论坛', sections_url(subdomain: ENV['SUBDOMAIN']) %>
    </li>
    <li>
      <%= link_to '应用下载', download_url(subdomain: ENV['SUBDOMAIN']) %>
    </li>
  </ul>

  <ul class="nav navbar-nav navbar-right">
    <%= render_cell :layouts, :login, user: current_user %>
  </ul>
</div>
