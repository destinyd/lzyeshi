<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= display_meta_tags default_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= csrf_meta_tags %>
    <!--[if lte IE 6]>
    <%= stylesheet_link_tag "bsie/bootstrap-ie6.min", "ie6", :media => "all" %>
    <![endif]-->
    <!--[if lte IE 7]>
    <%= stylesheet_link_tag "bsie/ie6", :media => "all" %>
    <![endif]-->
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>" data-spy="scroll" data-target=".bs-docs-sidebar">
    <div class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render "layouts/#{request.subdomains.first.to_s + "/" if !request.subdomains.blank? and request.subdomains.first != ENV['SUBDOMAIN']}navigation" %>
          <%#= render 'layouts/navigation' %>
        </div>
      </nav>
    </div>

    <!--[if IE 6]>
    <div id="notice_ie6">
      使用IE6浏览器可能导致无法正常使用本网站，请使用更高版本的IE、<a href="http://firefox.com.cn/">火狐</a>或Chrome浏览器访问。
    </div>
    <![endif]-->  

    <div id="main" role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <div class="breadcrumb">
                <%= render_crumbs separator: ' > ' %>
              </div>
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer id="footer">
            <%= render_cell :layouts, :footer %>
          </footer>
        </div>
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
    <%= javascript_include_tag "application" ,"#{ENV['FAYE']}.js" %>
    <!--[if lte IE 6]>
    <%= javascript_include_tag "ie6png", "bsie/bootstrap-ie6" %>
    <![endif]-->
    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <script type="text/javascript">
      (function ($) {
        $(document).ready(function() {
          if ($.isFunction($.bootstrapIE6)) $.bootstrapIE6($(document));
        });
      })(jQuery);
    </script>
    <% if current_user %>
      <script>
        $(function() {
          var faye = new Faye.Client('<%= ENV['FAYE'] %>');
          faye.subscribe("/n/<%= current_user.name %>", function(data) {
            humane.alter('您有新消息!<a href="/chat_messages/'+ data.chat_message_id + '">点击查看</a>');
          });
        });
      </script>
    <% end -%>
  </body>
</html>
