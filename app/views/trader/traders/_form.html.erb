<%= simple_form_for(@trader) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= render "fields_for",f: f %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
