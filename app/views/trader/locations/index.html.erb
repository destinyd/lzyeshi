<h1>Listing trader_locations</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trader_locations.each do |trader_location| %>
  <tr>
    <td><%= link_to 'Show', trader_location %></td>
    <td><%= link_to 'Edit', edit_trader_location_path(trader_location) %></td>
    <td><%= link_to 'Destroy', trader_location, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Location', new_trader_location_path %>
